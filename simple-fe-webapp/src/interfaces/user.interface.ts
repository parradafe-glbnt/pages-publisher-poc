/**
 * User interface
 * @prop name the main user name
 * @prop lastName the user last name
 * @prop age user age
 * @prop user international number (including prefix)
 * @interface
 */

export interface User {
    name : string;
    lastName : string;
    age : Number;
    phone : String;
}